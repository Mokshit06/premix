import type { Response } from 'express';

export async function redirect(res: Response, route: string) {
  // const App = await renderApp(route);
  // if ((App as any).notFound) {
  //   return res.status(404).send();
  // }
  // const html = handleRequest(App as () => ReactElement);
  // res.setHeader('content-type', 'text/html');
  // res.send(html);
  // if ()
}
