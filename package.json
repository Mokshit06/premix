{
  "name": "react-ssr",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "dev:server": "nodemon --watch build --exec node build/server",
    "dev": "concurrently \"yarn watch\" \"yarn dev:server\"",
    "watch": "node scripts/build",
    "clear": "rm -rf build public"
  },
  "dependencies": {
    "esbuild": "^0.11.11",
    "express": "^4.17.1",
    "preact": "^10.5.13",
    "preact-render-to-string": "^5.1.19",
    "react-router-dom": "^5.2.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.11",
    "concurrently": "^6.0.2",
    "nodemon": "^2.0.7"
  }
}
